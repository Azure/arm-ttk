{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "_artifactsLocation": {
            "type": "string",
            "defaultValue": "http://myweb.com/"  // this isn't valid for our repos
        },
        "_artifactsLocationSasToken": {
            "type": "securestring",
            "defaultValue": "hard coded token" //this shouldn't be a default (use a param if needed)
        }
    },
    "resources": [
    ],
    "outputs": {
    }
}
