(?>        # we don't want to flag a UDF that might be called "myListOfIps" so we need to check the char preceeding list*()
    \[|    # bracket (the function could be the first thing in the template expression)
    \(|    # paren (the function may be contained in another function)
    ,      # comma (the function may be contained in another function and not the first argument in that function)
)          # and the (?>  ) syntax says this is not included in the match because we need to check for expressions explicitly below
\s{0,}
list\w{1,}
\s{0,}
\(
